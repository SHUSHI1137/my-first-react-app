import navClass from './Navbar.module.css'

const Navbar = () => {
  return (
    <nav className={navClass.navstyle}>
      <p>Navbar</p>
      <button>Login</button>
    </nav>
  )
}

export default Navbar
